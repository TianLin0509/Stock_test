# API Keys — 绝对不能提交！
.streamlit/secrets.toml
secrets.toml

# Python
__pycache__/
*.pyc
.env

# IDE
.vscode/
.idea/

# Large data files (单个大文件排除，拆分的 part 文件允许上传)
data/history/all_daily.parquet

# User data (JSON persistence, not for git)
user_data/*.json

# Analysis archive (full text, for backtesting)
data/archive/
